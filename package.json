{
  "name": "wasteday",
  "version": "0.1.3",
  "private": true,
  "homepage": "https://erblack.me/wasteday/",
  "dependencies": {
    "image-to-slices": "^0.1.3",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "postversion": "npm run build && npm run publish && git push && git push --tags",
    "publish": "ghpages -p build/"
  },
  "keywords": [
    "game",
    "web",
    "canvas"
  ],
  "author": "ErBlack",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/ErBlack/wasteday.git"
  },
  "devDependencies": {
    "ghpages": "0.0.10",
    "phaser": "^3.13.0",
    "phaser3-plugin-isometric": "0.0.6"
  }
}
